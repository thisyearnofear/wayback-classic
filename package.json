{
  "name": "coupondj-monorepo",
  "private": true,
  "packageManager": "yarn@3.6.4",
  "workspaces": [
    "frontend",
    "backend",
    "onchain"
  ],
  "scripts": {
    "dev": "yarn workspace frontend dev",
    "build": "yarn workspaces foreach run build",
    "lint": "yarn workspaces foreach run lint"
  }
}